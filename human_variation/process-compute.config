process {
    executor = 'awsbatch'
    errorStrategy = 'retry'
    maxRetries = 3
    cpus = { min(32, 16 * task.attempt) }  // Scales up to a max of 32 CPUs
    memory = { min(128.GB, 30.GB * task.attempt) }  // Scales up to a max of 128GB
}
