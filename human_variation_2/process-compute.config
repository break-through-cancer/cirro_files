process {
    executor = 'awsbatch'
    errorStrategy = 'retry'
    maxRetries = 3
    cpus = { 16 * task.attempt }  // Scales up to a max of 32 CPUs
    memory = {  64.GB * task.attempt }  // Scales up to a max of 128GB
}
