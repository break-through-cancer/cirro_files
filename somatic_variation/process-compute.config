process {
    executor = 'awsbatch'
    errorStrategy = 'retry'
    maxRetries = 3
    cpus = 32
    memory = 256.GB
}

process dss {
    // Overwrite thread limit of 4 in original code
    cpus = 4
}